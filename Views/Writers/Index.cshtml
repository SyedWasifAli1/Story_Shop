@model IEnumerable<Book_kharido.Models.Writer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WriteName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WriteName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WritetId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WritetId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WritetId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
